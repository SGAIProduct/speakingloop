# SpeakLoop Web

The active web application is served from the repository root:

- `index.html`
- `src/main.js`
- `src/styles.css`
- `server.mjs`

It includes manual vocabulary capture, vocabulary cards, review scheduling, speaking-practice targets, and the local Qwen model router.
